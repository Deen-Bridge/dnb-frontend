//usage for gettiing or fetch reels;
useEffect(() => {
  const getReels = async () => {
    const data = await fetchReels();
    setReels(data.reels); // Assuming response has a `reels` array
  };

  getReels();
}, []);




//usage for posting reels
const handleUpload = async () => {
  const result = await uploadReel(videoFile, description, category);
  if (result?.success) {
    alert("Reel uploaded!");
  }
};
