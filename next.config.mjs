/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.ibb.co", "media.istockphoto.com", "images.unsplash.com", "res.cloudinary.com"],
  },
};

export default nextConfig;
